<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bai8">

    <!-- Khai báo quyền nếu sử dụng gọi điện, gửi SMS -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="Bai8"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bai8">

        <!-- Khai báo SendSMSActivity -->
        <activity android:name="com.example.bai8.SendSMSActivity"
            android:exported="false" />

        <!-- Khai báo CallPhoneActivity -->
        <activity android:name="com.example.bai8.CallPhoneActivity"
            android:exported="false" />

        <!-- Khai báo MainActivity -->
        <activity android:name="com.example.bai8.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
